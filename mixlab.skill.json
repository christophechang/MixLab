{
  "name": "mixlab",
  "description": "AI-powered DJ crate assistant. Reads a Rekordbox collection, filters played tracks, clusters by genre, and generates mix concepts via LLM.",
  "triggers": [
    {
      "pattern": "^!mixlab genres$",
      "command": "./mixlab --genres",
      "description": "Show cached genre availability from last run (no API calls)"
    },
    {
      "pattern": "^!mixlab ([a-zA-Z_&]+)$",
      "command": "./mixlab --genre \"$1\" --export-playlists",
      "description": "Run pipeline scoped to a single genre (Anthropic Stage 2)"
    },
    {
      "pattern": "^!mixlab ([a-zA-Z_&]+) (\\d+)$",
      "command": "./mixlab --genre \"$1\" --duration $2 --export-playlists",
      "description": "Run pipeline for a genre with a target set duration (minutes)"
    }
  ],
  "env": [
    "CHANGSTA_API_KEY",
    "CATALOG_API_URL",
    "DISCORD_BOT_TOKEN",
    "DISCORD_GUILD_ID",
    "MIXLAB_DISCORD_CHANNEL",
    "MINIMAX_API_KEY",
    "GROQ_API_KEY",
    "GEMINI_API_KEY",
    "OPENROUTER_API_KEY",
    "ANTHROPIC_API_KEY"
  ],
  "working_directory": "/Users/christophechang/OpenClaw/Automations/MixLab"
}
